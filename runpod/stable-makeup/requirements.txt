runpod>=1.7.0
# Do NOT install torch here — use the base image's CUDA-matched torch (see catvton-flux note).
diffusers>=0.27.0
transformers>=4.40.0
accelerate>=0.30.0
opencv-python-headless>=4.9.0
Pillow>=10.4.0
numpy>=1.26.0
huggingface_hub>=0.25.0
# The Stable-Makeup repo (Xiaojiu-z/Stable-Makeup) is cloned onto PYTHONPATH in
# the Dockerfile; SPIGA / face-landmark deps come from that repo's requirements.
