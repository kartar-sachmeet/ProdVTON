runpod>=1.7.0
# DO NOT install torch here — the base image ships torch+torchaudio built for CUDA 12.4.
# Reinstalling torch from PyPI pulls a cu121 wheel and mismatches torchaudio (cu124),
# which crashes on import (transformers→torchaudio CUDA check). Use the base image's torch.
diffusers>=0.32.0
transformers>=4.44.0
accelerate>=0.34.0
sentencepiece>=0.2.0
protobuf>=4.25.0
peft>=0.13.0
Pillow>=10.4.0
huggingface_hub>=0.25.0
numpy>=1.26.0
opencv-python-headless>=4.9.0
scipy>=1.13.0
av>=12.0.0  # PyAV — transformers/torchvision import it during model import
# AutoMasker needs DensePose (detectron2) — installed from source in the Dockerfile.
